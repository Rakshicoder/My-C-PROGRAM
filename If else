include <stdio.h>

int main(){
    int num;
    printf("Enter a number:");
    scanf("%d",&sun);
    if(num%2==0){
        printf("The number is Even ");
    }else{
        printf("The number is odd");
    
    return 0
    }
